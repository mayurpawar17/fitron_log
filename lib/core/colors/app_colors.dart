import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const bg = Color(0xFF0B0B0B);
  static const card = Color(0xFF1A1A1A);
  static const accent = Color(0xFF1CFF00); // neon green
  static const textSecondary = Color(0xFF9E9E9E);
}
