import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const bg = Color(0xFF0B0B0B);
  static const bg2 = Color(0xFF1a1a1a);
  static const bgSecondary = Color(0xFF1f1f1f);
  static const card = Color(0xFF1A1A1A);
  // static const accent = Color(0xFF1CFF00); // neon green
  static const accent = Color(0xFF2EE6A6); // pista
  // static const accent = Color(0xFF653ff4); // violet
  // static const accentPurple = Color(0xFF653ff4); // neon green
  static const textSecondary = Color(0xFF9E9E9E);
}
